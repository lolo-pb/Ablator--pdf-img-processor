{
  "name": "bank-reconciliation-platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @app/web",
    "build": "npm run build --workspace @app/web",
    "typecheck": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "typescript": "^5.9.2"
  },
  "devDependencies": {
    "@types/node": "^24.7.0",
    "@types/react": "^19.1.12",
    "@types/react-dom": "^19.1.9",
    "vitest": "^3.2.4"
  }
}
